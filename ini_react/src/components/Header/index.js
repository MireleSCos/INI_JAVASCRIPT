import React, {Component} from 'react';
import './styles.css';

/*Pode-se criar funções que extend de components, 
não apenas classes, isso é nomeado de styled-components.
Ultiliza as classes quando usar estado */

const Header = () => <header id="main-header"> Exporcard</header>

export default Header;